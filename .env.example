# PORT is the port number where the server will run.
PORT=3000

# DB_BASE_PATH is the folder that contains the JSON files that act like our database.
DB_BASE_PATH=./db

# Every new user starts with this amount of money.
STARTING_BALANCE=500

# Login tokens expire after this many minutes.
TOKEN_EXPIRATION_MINUTES=60
